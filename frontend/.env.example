# Base URL of the backend API.
# Leave blank in development — the Vite dev server proxies /api to localhost:8000.
# In production set this to your deployed backend URL (e.g. https://api.example.com).
VITE_API_URL=
